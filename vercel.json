{
    "builds": [
        {
            "src": "myproject/wsgi.py",
            "use": "@vercel/python",
            "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "myproject/wsgi.py"
        }
    ],
    "buildCommand": "pip install -r requirements.txt && python manage.py migrate && echo 'DJANGO_SUPERUSER_USERNAME: $DJANGO_SUPERUSER_USERNAME' && echo 'DJANGO_SUPERUSER_EMAIL: $DJANGO_SUPERUSER_EMAIL' && python manage.py createsuperuser --noinput"
}
